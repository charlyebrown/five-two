<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Five Two" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Five Two" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <% if current_user %>
      <script type ='javascript'>
        App.Globals.userId = <%= current_user.id %>
      </script>
    <% end %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <div id="user-display"><span class='add'>Add Event</span></div>
       <% if current_user %>
       <div id='event-form'></div>
       <% end %>
       <%= yield %>
    </main>
  </body>
</html>

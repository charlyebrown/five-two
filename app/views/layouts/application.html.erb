<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Five Two" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Five Two" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <link href="../assets/stylesheets/small-business.css" rel="stylesheet">
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <% if current_user %>
      <%= javascript_tag do %>
      window.userID = "<%=j current_user.id.to_s %>";
      <% end %>
    <% end %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>

        <!-- Page Content -->
    <div class="container">

        <!-- Heading Row -->
        <div class="row">
            <div class="col-md-8">
                <img class="img-responsive img-rounded" src="http://tomorrowisadayaway.com/wp-content/uploads/2013/01/52by52.jpg" alt="">
            </div>
            <!-- /.col-md-8 -->
            <div class="col-md-4 intro">
                <p>FIVE and TWO allows you to create visualizations of your
                life in terms of weeks. </p>

                <p>Simply add events with starting and ending
                dates and select the type of graphic you'd like to see!</p>
            </div>
            <!-- /.col-md-4 -->
        </div>
        <!-- /.row -->

        <hr>


        <!-- Content Row -->
        <div class="row">
    <main role="main">
       <%= render 'layouts/messages' %>
       <% if current_user %>
         <div id="user-display"></div>
         <div id="event-container"></div>
       <% end %>
       <%= yield %>
    </main>
        </div>
        <!-- /.row -->

        <!-- Footer -->
        <footer>
            <div class="row">
                <div class="col-lg-12">
                    <p>Copyright &copy; Your Website 2014</p>
                </div>
            </div>
        </footer>

    </div>

  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Five Two" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Five Two" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <script src="http://d3js.org/d3.v3.min.js"></script>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <script src="http://cdnjs.cloudflare.com/ajax/libs/randomcolor/0.1.1/randomColor.js"></script>
    <script src="http://cdnjs.cloudflare.com/ajax/libs/randomcolor/0.1.1/randomColor.min.js"></script>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <% if current_user %>
      <%= javascript_tag do %>
        window.userID = "<%=j current_user.id.to_s %>";
      <% end %>
    <% end %>

        <!-- Page Content -->
    <div class="container">

        <!-- Heading Row -->
        <div class="row">
            <div class="col-md-8 head_row">
                <img class="img-responsive img-rounded" src="http://flothemes.com/test/blanco/wp-content/uploads/sites/5/2014/01/0007_macleod_raw1462-950x500.jpg" alt="">
            </div>
            <!-- /.col-md-8 -->
            <div class="col-md-4 intro head_row">
                <p>FIVE and TWO allows you to create visualizations of your
                life in terms of weeks. </p>

                <p>Simply add events with starting and ending
                dates and select the type of graphics you'd like to see, and Voila!</p>
            </div>
            <!-- /.col-md-4 -->
        </div>
        <!-- /.row -->

        <hr>


        <!-- Content Row -->
        <div class="row">
    <main role="main">
       <%= render 'layouts/messages' %>
       <% if current_user %>
        <div id="user-display">
          <span class='add_event' id='add_event'>Add an Event</span>
          <span class='add_event' id="bubbles">Show Bubbles</span>
          <span class='add_event' id="bars">Show Bars</span>

          <div id="event-container">
          </div>
        </div>
        <div class="svg_display"></div>
       <% end %>
       <%= yield %>
    </main>
        </div>
        <!-- /.row -->

        <!-- Footer -->
        <footer>
            <div class="row">
                <div class="col-lg-12">
                    <p>Copyright &copy; Charlye Brown 2014</p>
                </div>
            </div>
        </footer>

    </div>

  </body>
</html>
